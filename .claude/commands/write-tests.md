Use the unit-test-writer agent to write unit tests for the smart contract at: $ARGUMENTS

If $ARGUMENTS is a contract name (e.g. "router", "factory", "escrow"), locate the contract under the `contracts/` directory. If it is a path, use it directly.
