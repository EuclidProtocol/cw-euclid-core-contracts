[package]
name = "schema-to-proto"
version = "0.1.0"
edition = "2021"
description = "Convert CosmWasm JSON Schema files to Protocol Buffer (.proto) definitions"

[[bin]]
name = "schema-to-proto"
path = "src/main.rs"

[dependencies]
serde_json = "1"
glob = "0.3"
regex = "1"
